package {{package}}.service;

import org.junit.jupiter.api.BeforeEach;
import org.junit.jupiter.api.Test;
import org.junit.jupiter.api.DisplayName;
{{#imports}}
import {{import}};
{{/imports}}
import {{apiPackage}}.protocol.ApiRequest;
import {{apiPackage}}.protocol.ApiResponse;

import static org.junit.jupiter.api.Assertions.*;

/**
 * Tests for {{classname}}Service.
 * Auto-generated from OpenAPI specification — customize as needed.
 */
@DisplayName("{{classname}} API Tests")
class {{classname}}ServiceTest {

    // TODO: Initialize your service implementation
    // private {{classname}}Service service;

    @BeforeEach
    void setUp() {
        // service = new {{classname}}ServiceImpl();
    }

{{#operations}}
{{#operation}}
    @Test
    @DisplayName("{{httpMethod}} {{path}} — {{summary}}")
    void test{{#lambda.titlecase}}{{operationId}}{{/lambda.titlecase}}() {
        // Given
        {{#allParams}}
        // {{dataType}} {{paramName}} = ...;
        {{/allParams}}

        // When
        // {{#returnType}}{{.}} result = {{/returnType}}service.{{operationId}}({{#allParams}}{{paramName}}{{^-last}}, {{/-last}}{{/allParams}});

        // Then
        {{#returnType}}
        // assertNotNull(result);
        {{/returnType}}
        fail("Test not implemented — replace with your assertions");
    }

{{/operation}}
{{/operations}}
}
