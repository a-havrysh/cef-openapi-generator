package {{apiPackage}}.exception

/**
 * HTTP 400 Bad Request.
 *
 * Thrown when the server cannot process the request due to client-side errors
 * (invalid syntax, malformed parameters, failed validation, etc.).
 */
open class BadRequestException(
    message: String,
    cause: Throwable? = null
) : ApiException(400, message, cause)
