package {{package}}.service

{{#imports}}
import {{import}}
{{/imports}}
import {{apiPackage}}.protocol.ApiRequest
import {{apiPackage}}.protocol.ApiResponse
import org.junit.jupiter.api.BeforeEach
import org.junit.jupiter.api.Test
import org.junit.jupiter.api.DisplayName
import org.junit.jupiter.api.Assertions.*

/**
 * Tests for {{classname}}Service.
 * Auto-generated from OpenAPI specification — customize as needed.
 */
@DisplayName("{{classname}} API Tests")
class {{classname}}ServiceTest {

    // TODO: Initialize your service implementation
    // private lateinit var service: {{classname}}Service

    @BeforeEach
    fun setUp() {
        // service = {{classname}}ServiceImpl()
    }

{{#operations}}
{{#operation}}
    @Test
    @DisplayName("{{httpMethod}} {{path}} — {{summary}}")
    fun `test {{operationId}}`() {
        // Given
        {{#allParams}}
        // val {{paramName}}: {{dataType}}{{^required}}?{{/required}} = TODO()
        {{/allParams}}

        // When
        // {{#returnType}}val result = {{/returnType}}service.{{operationId}}({{#allParams}}{{paramName}}{{^-last}}, {{/-last}}{{/allParams}})

        // Then
        {{#returnType}}
        // assertNotNull(result)
        {{/returnType}}
        fail("Test not implemented — replace with your assertions")
    }

{{/operation}}
{{/operations}}
}
